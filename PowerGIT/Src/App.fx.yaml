App As appinfo:
    BackEnabled: =false
    OnStart: |-
        =ClearCollect(
            colNavbar,
            {}
        )

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

